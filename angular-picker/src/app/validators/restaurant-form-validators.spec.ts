import { RestaurantFormValidators } from './restaurant-form-validators';

describe('RestaurantFormValidators', () => {
  it('should create an instance', () => {
    expect(new RestaurantFormValidators()).toBeTruthy();
  });
});
